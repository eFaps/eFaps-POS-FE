import { PosCurrencyPipe } from './pos-currency.pipe';

describe('PosCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new PosCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
