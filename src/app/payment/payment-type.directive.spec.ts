import { PaymentTypeDirective } from './payment-type.directive';

describe('PaymentTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new PaymentTypeDirective(null);
    expect(directive).toBeTruthy();
  });
});
