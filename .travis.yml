addons:
  chrome: stable

language: node_js
node_js:
  - node
  - lts/*

cache:
  directories:
    - "node_modules"

#before_install:
 #- export CHROME_BIN=chromium-browser

install:
  - npm install

script:
  - npm run _build-css
  - npm test
